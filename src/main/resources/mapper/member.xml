<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ssafy.member">
   <select id="login" parameterType="map" resultType="com.ssafy.web.dto.Member">
      select * from member where userid = #{id} and userpwd = #{pw}
   </select>
   
   <select id="check" parameterType="map" resultType="string">
      
      select userpwd from member where userid = #{id} and username = #{name}
   </select>
   
   <insert id="signup" parameterType="com.ssafy.web.dto.Member">
        insert into member (userid, userpwd, username, hakgun, cafe, hospital, theater,depart,mac,subway) values (#{userid}, #{userpwd}, #{username},#{hakgun}, #{cafe}, #{hospital}, #{theater},#{depart},#{mac},#{subway})
    </insert>
    
    <update id="modify" parameterType="com.ssafy.web.dto.Member">
       update member set username = #{username},userpwd = #{userpwd},hakgun = #{hakgun},cafe = #{cafe}, hospital = #{hospital},
                     theater = #{theater}, depart = #{depart}, mac = #{mac}, subway = #{subway} where userid = #{userid}
    </update>
    
    <delete id="delete" parameterType="string">
       delete from member where userid = #{id}
    </delete>
    
    <select id="search" resultType="com.ssafy.web.dto.Member">
       select * from member
    </select>
    
  <!--    <select id="favorite" resultType="com.ssafy.web.dto.Favorite">
       insert into member (userid, userpwd, username) values (#{userid}, #{userpwd}, #{username})
    </select>   -->
</mapper>