<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ssafy.store">
	<select id="storeget" parameterType="map" resultType="com.ssafy.web.dto.Store">
		Select *
		From store
		where sido = #{sido} and gugun = #{gugun} and dong = #{dong}
		ORDER BY no
	</select>
	
	<select id="storeget2" parameterType="map" resultType="com.ssafy.web.dto.Store">
		Select *
		From store
		where sido = #{sido} and gugun = #{gugun}
		ORDER BY no
	</select>
</mapper>
