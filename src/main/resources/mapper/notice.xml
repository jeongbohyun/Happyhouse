<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.ssafy.web.repository.NoticeRepository">
 	
 	<insert id="write" parameterType="com.ssafy.web.dto.Notice">
		insert into notice (userid, subject, content, regtime)
		values (#{userid}, #{subject}, #{content}, now())
	</insert>
 	
 	
 	<select id="list" parameterType="map" resultType="com.ssafy.web.dto.Notice">
		select articleno, userid, subject, content, regtime
		from notice
		<if test="word != null and word != ''">
			<if test="key == 'subject'">
				where subject like concat('%', #{word}, '%')
			</if>
			<if test="key != 'subject'">
				where ${key} = #{word}
			</if>
		</if>
		order by articleno desc
		limit #{start}, #{spp}
	</select>
	
	<select id="getTotalCount" parameterType="map" resultType="int">
		select count(articleno)
		from notice
		<if test="word != null and word != ''">
			<if test="key == 'subject'">
				where subject like concat('%', #{word}, '%')
			</if>
			<if test="key != 'subject'">
				where ${key} = #{word}
			</if>
		</if>
	</select>
	
	<select id="get" parameterType="int" resultType="com.ssafy.web.dto.Notice">
		select articleno, userid, subject, content, regtime
		from notice
		where articleno = #{articleno}
	</select>
	
	<update id="modify" parameterType="com.ssafy.web.dto.Notice">
		update notice
		set subject = #{subject}, content = #{content}
		where articleno = #{articleno}
	</update>
	
	<delete id="delete" parameterType="int">
	delete from notice
	where articleno = #{articleno}
	</delete>
	
 </mapper>